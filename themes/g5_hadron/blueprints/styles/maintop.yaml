name: MainTop Colors
description: MainTop colors for the Hadron theme
type: section

form:
  fields:

    background:
      type: input.colorpicker
      label: Background
      default: "#131315"
    text-color:
      type: input.colorpicker
      label: Text
      default: "#bbbbbb"